name: Flutter APK Build

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-apk:
    name: Build APK (debug + release)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.24.x
          cache: true

      - name: Generate Gradle wrapper
        run: |
          cd android
          curl -sL -o /tmp/gradle.zip https://services.gradle.org/distributions/gradle-8.10.2-bin.zip
          mkdir -p /tmp/gradle-install
          unzip -qo /tmp/gradle.zip -d /tmp/gradle-install
          /tmp/gradle-install/gradle-8.10.2/bin/gradle wrapper --gradle-version 8.10.2
          ls -la gradle/wrapper/

      - name: Dart pub get
        run: dart pub get

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Build debug APK
        run: flutter build apk --debug --no-pub

      - name: Build release APK (unsigned)
        run: flutter build apk --release --no-pub

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: apk-builds
          path: build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          files: build/app/outputs/flutter-apk/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
